import "package:flutter/material.dart";

class CardHasilPemilihan extends StatelessWidget {
  const CardHasilPemilihan({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Card();
  }
}